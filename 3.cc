#include <bits/stdc++.h>
using namespace std;
strcut Classes {
    int begin, end;
    Classes(int a, int b) {
        begin = a; end = b;
    }
    bool operator<(Classes& rhs) {
        return this->begin < rhs.begin;
    }
}

int main() {
    int n;
    cin >> n;
    for (int i = 0; i < n; i++) {

    }
 
}